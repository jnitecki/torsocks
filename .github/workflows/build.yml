name: Build
on:
  push:
    workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # --------------------------------------------------------------------
      # 1. Checkout the repo
      # --------------------------------------------------------------------
      - name: Checkout code
        uses: actions/checkout@v3

      # --------------------------------------------------------------------
      # 2. Install build‑time dependencies
      # --------------------------------------------------------------------
      - name: Install autotools & compiler
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            autoconf \
            automake \
            libtool \
            pkg-config  # useful for many autotools projects

      # --------------------------------------------------------------------
      # 3. Run the autotools pipeline
      # --------------------------------------------------------------------
      - name: AutoGen
        run: ./autogen.sh

      - name: Configure
        run: ./configure

      # --------------------------------------------------------------------
      # 4. Build with GCC (via make)
      # --------------------------------------------------------------------
      - name: Build
        run: make -j$(nproc)

      # --------------------------------------------------------------------
      # 5. Create upload folder and copy files
      # --------------------------------------------------------------------
      - name: Create upload folder and copy files
        run: mkdir -p "${RUNNER_TEMP}/upload" && cp src/lib/.libs/libtorsocks.so "${RUNNER_TEMP}/upload/" && cp src/bin/torsocks "${RUNNER_TEMP}/upload/"

      - name: List files in source and copied ones
        run: ls -al "${RUNNER_TEMP}/upload" && ls -al src/lib/.libs/ && ls -al src/bin/torsocks

      # --------------------------------------------------------------------
      # 5. (Optional) Upload build artifacts
      # --------------------------------------------------------------------
      - name: Upload binaries
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: binaries
          path: |
            ${RUNNER_TEMP}/upload/